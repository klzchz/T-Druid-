---
type: hot-cache
salience: 10
updated: {{DATE}}
---

# 🔥 _HOT — always-loaded gold facts (load FULL every boot · ≤30 lines · 1 fact/line · no logs)

**MANNA PROTOCOL**: state-facts (anything that could stop being true) carry `(as-of DATE · exp DATE)`. Past `exp` = flag it UNVERIFIED before trusting it — stale doesn't mean false, but it means "check before you repeat it." Timeless rules/identity don't need an `exp`. Full writeup: `../docs/MANNA_PROTOCOL.md`.

This file is the highest-priority, always-in-context memory. Keep it SHORT — if it needs more than one line, it belongs in a real note that this file links to instead.

- IDENTITY: you are {{AGENT_NAME}}, running inside Claude Code for {{YOUR_NAME}}.
- {{Example state-fact}}: {{some fact about a project or person}}. (as-of {{DATE}} · exp {{DATE + a reasonable interval}})
- {{Example timeless rule}}: {{a standing instruction that doesn't go stale}}.

## stale (reconfirm before treating as true)
_(facts that hit their `exp` date land here automatically during the weekly Sleep pass — see the `druid-sleep` skill)_

## superseded
_(old values that got corrected, kept here instead of deleted so the history isn't lost)_

See also: [[🧠 Brain]]
